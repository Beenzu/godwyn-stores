# Production secrets — NEVER commit .env to GitHub.
JWT_SECRET=replace-with-a-random-64-character-secret
SETUP_KEY=replace-with-a-private-setup-key
NODE_ENV=production
PORT=3000

# On Render, mount a persistent disk at /var/data and set:
DATA_DIR=/var/data
UPLOAD_DIR=/var/data/uploads
